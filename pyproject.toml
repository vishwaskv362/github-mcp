[project]
name = "github-mcp"
version = "0.1.0"
description = "An MCP server that wraps the GitHub REST API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "mcp>=1.25.0",
]
